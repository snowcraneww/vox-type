{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow VoxType windows to invoke app commands and listen for local push-to-talk events.",
  "windows": ["main", "dictation-overlay"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
