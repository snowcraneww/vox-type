# OpenSpec

OpenSpec will hold approved product and architecture changes for VoxType.

Expected layout:

```text
openspec/
  changes/
    <change-id>/
      proposal.md
      tasks.md
      specs/
```

Use OpenSpec after discovery and research produce a concrete product or architecture proposal. Do not implement product features directly from chat notes.